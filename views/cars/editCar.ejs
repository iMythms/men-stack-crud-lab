<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/global.css" />
		<title>Edit <%= car.model %></title>
	</head>
	<body>
		<h1>Edit <%= car.model %></h1>
		<form action="/cars/<%= car._id %>/?_method=PUT" method="POST">
			<div>
				<label for="type">Type</label>
				<select name="type" id="type" required>
					<option value="<%= car.type %>" disabled selected>
						<%= car.type %>
					</option>
					<option value="SUV">SUV</option>
					<option value="SEDAN">Sedan</option>
					<option value="TRUCK">Truck</option>
					<option value="SPORT">Sport</option>
				</select>
			</div>
			<div>
				<label for="brand">Brand</label>
				<input
					type="text"
					name="brand"
					id="brand"
					required
					placeholder="Car Brand"
				/>
			</div>
			<div>
				<label for="color">Car Color</label>
				<input
					type="text"
					name="color"
					id="color"
					required
					placeholder="Please select a color"
				/>
			</div>
      <div>
        <label for="year">Car Model</label>
        <select name="year" id="year" required></select>
          <option value="<%= car.year %>" disabled selected><%= car.year %></option>
          <% for (let year = 2000; year <= 2024; year++) { %>
            <option value="<%= year %>"><%= year %></option>
          <% } %>
        </select>
      </div>
		</form>
	</body>
</html>
